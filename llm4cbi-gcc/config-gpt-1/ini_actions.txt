addQualifier;inserting a qualifier (i.e., volatile, const, and restrict) of the variable in the list ['p', 'pj', 'u']
remQualifier;removing a qualifier (i.e., volatile, const, and restrict) of the variable in the list ['p', 'pj', 'u']
addModifier;inserting a modifier (i.e., long, short, signed, and unsigned) of the variable in the list ['p', 'pj', 'u']
remModifier;removing a modifier (i.e., long, short, signed, and unsigned) of the variable in the list ['p', 'pj', 'u']
repContant;replacing a constant with another valid one of a variable in the list ['p', 'pj', 'u'] 
repBinary;replacing a binary operator with another valid one on the variables in the list ['p', 'pj', 'u']
repUnary;replacing an unary operator on the variables in the list ['p', 'pj', 'u']
remUnary;removing an unary operator on the variables in the list ['p', 'pj', 'u']
repVar;replacing a variable in the list ['p', 'pj', 'u'] with another valid existing one in the list ['pj', 'u']  
addIf;inserting a branch (i.e., if) statement with length  1 - 5  lines in the statement body only between line 44 and line 48 in the original given program and reusing the variables in the list ['pj', 'u'] 
addWhile;inserting a loop (i.e., while or for) statement with length  1 - 5  lines in the statement body only between line 44 and line 48 in the original given program and reusing the variables in the list ['pj', 'u'] 
addFunction;inserting a function call with length  1 - 5  lines in the statement body only between line 44 and line 48 in the original given program and reusing the variables in the list ['pj', 'u'] 
addGoto;inserting a goto statement with length  1 - 5  lines in the statement body only between line 44 and line 48 in the original given program and reusing the variables in the list ['pj', 'u'] 
