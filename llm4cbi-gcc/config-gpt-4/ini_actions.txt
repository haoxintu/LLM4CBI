addQualifier;inserting a qualifier (i.e., volatile, const, and restrict) of the variable in the list ['a', 'b', 'p2']
remQualifier;removing a qualifier (i.e., volatile, const, and restrict) of the variable in the list ['a', 'b', 'p2']
addModifier;inserting a modifier (i.e., long, short, signed, and unsigned) of the variable in the list ['a', 'b', 'p2']
remModifier;removing a modifier (i.e., long, short, signed, and unsigned) of the variable in the list ['a', 'b', 'p2']
repContant;replacing a constant with another valid one of a variable in the list ['a', 'b', 'p2'] 
repBinary;replacing a binary operator with another valid one on the variables in the list ['a', 'b', 'p2']
repUnary;replacing an unary operator on the variables in the list ['a', 'b', 'p2']
remUnary;removing an unary operator on the variables in the list ['a', 'b', 'p2']
repVar;replacing a variable in the list ['a', 'b', 'p2'] with another valid existing one in the list ['a', 'b']  
addIf;inserting a branch (i.e., if) statement with length  1 - 5  lines in the statement body only between line 13 and line 20 in the original given program and reusing the variables in the list ['a', 'b'] 
addWhile;inserting a loop (i.e., while or for) statement with length  1 - 5  lines in the statement body only between line 13 and line 20 in the original given program and reusing the variables in the list ['a', 'b'] 
addFunction;inserting a function call with length  1 - 5  lines in the statement body only between line 13 and line 20 in the original given program and reusing the variables in the list ['a', 'b'] 
addGoto;inserting a goto statement with length  1 - 5  lines in the statement body only between line 13 and line 20 in the original given program and reusing the variables in the list ['a', 'b'] 
