addQualifier;inserting a qualifier (i.e., volatile, const, and restrict) of the variable in the list ['b', 'i', 'j']
remQualifier;removing a qualifier (i.e., volatile, const, and restrict) of the variable in the list ['b', 'i', 'j']
addModifier;inserting a modifier (i.e., long, short, signed, and unsigned) of the variable in the list ['b', 'i', 'j']
remModifier;removing a modifier (i.e., long, short, signed, and unsigned) of the variable in the list ['b', 'i', 'j']
repContant;replacing a constant with another valid one of a variable in the list ['b', 'i', 'j'] 
repBinary;replacing a binary operator with another valid one on the variables in the list ['b', 'i', 'j']
repUnary;replacing an unary operator on the variables in the list ['b', 'i', 'j']
remUnary;removing an unary operator on the variables in the list ['b', 'i', 'j']
repVar;replacing a variable in the list ['b', 'i', 'j'] with another valid existing one in the list ['b', 'g', 'i']  
addIf;inserting a branch (i.e., if) statement with length  1 - 5  lines in the statement body only between line 36 and line 39 in the original given program and reusing the variables in the list ['b', 'g', 'i'] 
addWhile;inserting a loop (i.e., while or for) statement with length  1 - 5  lines in the statement body only between line 36 and line 39 in the original given program and reusing the variables in the list ['b', 'g', 'i'] 
addFunction;inserting a function call with length  1 - 5  lines in the statement body only between line 36 and line 39 in the original given program and reusing the variables in the list ['b', 'g', 'i'] 
addGoto;inserting a goto statement with length  1 - 5  lines in the statement body only between line 36 and line 39 in the original given program and reusing the variables in the list ['b', 'g', 'i'] 
