addQualifier;inserting a qualifier (i.e., volatile, const, and restrict) of the variable in the list ['b', 'd', 'i']
remQualifier;removing a qualifier (i.e., volatile, const, and restrict) of the variable in the list ['b', 'd', 'i']
addModifier;inserting a modifier (i.e., long, short, signed, and unsigned) of the variable in the list ['b', 'd', 'i']
remModifier;removing a modifier (i.e., long, short, signed, and unsigned) of the variable in the list ['b', 'd', 'i']
repContant;replacing a constant with another valid one of a variable in the list ['b', 'd', 'i'] 
repBinary;replacing a binary operator with another valid one on the variables in the list ['b', 'd', 'i']
repUnary;replacing an unary operator on the variables in the list ['b', 'd', 'i']
remUnary;removing an unary operator on the variables in the list ['b', 'd', 'i']
repVar;replacing a variable in the list ['b', 'd', 'i'] with another valid existing one in the list ['b', 'd', 'e']  
addIf;inserting a branch (i.e., if) statement with length  1 - 5  lines in the statement body only between line 28 and line 29 in the original given program and reusing the variables in the list ['b', 'd', 'e'] 
addWhile;inserting a loop (i.e., while or for) statement with length  1 - 5  lines in the statement body only between line 28 and line 29 in the original given program and reusing the variables in the list ['b', 'd', 'e'] 
addFunction;inserting a function call with length  1 - 5  lines in the statement body only between line 28 and line 29 in the original given program and reusing the variables in the list ['b', 'd', 'e'] 
addGoto;inserting a goto statement with length  1 - 5  lines in the statement body only between line 28 and line 29 in the original given program and reusing the variables in the list ['b', 'd', 'e'] 
