addQualifier;inserting a qualifier (i.e., volatile, const, and restrict) of the variable in the list ['a', 'd', 'g']
remQualifier;removing a qualifier (i.e., volatile, const, and restrict) of the variable in the list ['a', 'd', 'g']
addModifier;inserting a modifier (i.e., long, short, signed, and unsigned) of the variable in the list ['a', 'd', 'g']
remModifier;removing a modifier (i.e., long, short, signed, and unsigned) of the variable in the list ['a', 'd', 'g']
repContant;replacing a constant with another valid one of a variable in the list ['a', 'd', 'g'] 
repBinary;replacing a binary operator with another valid one on the variables in the list ['a', 'd', 'g']
repUnary;replacing an unary operator on the variables in the list ['a', 'd', 'g']
remUnary;removing an unary operator on the variables in the list ['a', 'd', 'g']
repVar;replacing a variable in the list ['a', 'd', 'g'] with another valid existing one in the list ['a', 'b']  
addIf;inserting a branch (i.e., if) statement with length  1 - 5  lines in the statement body only between line 26 and line 27 in the original given program and reusing the variables in the list ['a', 'b'] 
addWhile;inserting a loop (i.e., while or for) statement with length  1 - 5  lines in the statement body only between line 26 and line 27 in the original given program and reusing the variables in the list ['a', 'b'] 
addFunction;inserting a function call with length  1 - 5  lines in the statement body only between line 26 and line 27 in the original given program and reusing the variables in the list ['a', 'b'] 
addGoto;inserting a goto statement with length  1 - 5  lines in the statement body only between line 26 and line 27 in the original given program and reusing the variables in the list ['a', 'b'] 
