addQualifier;inserting a qualifier (i.e., volatile, const, and restrict) of the variable in the list ['c', 'd', 'h']
remQualifier;removing a qualifier (i.e., volatile, const, and restrict) of the variable in the list ['c', 'd', 'h']
addModifier;inserting a modifier (i.e., long, short, signed, and unsigned) of the variable in the list ['c', 'd', 'h']
remModifier;removing a modifier (i.e., long, short, signed, and unsigned) of the variable in the list ['c', 'd', 'h']
repContant;replacing a constant with another valid one of a variable in the list ['c', 'd', 'h'] 
repBinary;replacing a binary operator with another valid one on the variables in the list ['c', 'd', 'h']
repUnary;replacing an unary operator on the variables in the list ['c', 'd', 'h']
remUnary;removing an unary operator on the variables in the list ['c', 'd', 'h']
repUnary;removing an unary operator on the variables in the list ['c', 'd', 'h'] only between line 17 and line 20 in the original given program (if any)
repVar;replacing a variable in the list ['c', 'd', 'h'] with another valid existing one in the list ['a', 'c', 'd']  
addIf;inserting a branch (i.e., if) statement with length  1 - 5  lines in the statement body only between line 17 and line 20 in the original given program and reusing the variables in the list ['a', 'c', 'd'] 
addWhile;inserting a loop (i.e., while or for) statement with length  1 - 5  lines in the statement body only between line 17 and line 20 in the original given program and reusing the variables in the list ['a', 'c', 'd'] 
addFunction;inserting a function call with length  1 - 5  lines in the statement body only between line 17 and line 20 in the original given program and reusing the variables in the list ['a', 'c', 'd'] 
addGoto;inserting a goto statement with length  1 - 5  lines in the statement body only between line 17 and line 20 in the original given program and reusing the variables in the list ['a', 'c', 'd'] 
