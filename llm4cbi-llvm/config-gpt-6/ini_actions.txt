addQualifier;inserting a qualifier (i.e., volatile, const, and restrict) of the variable in the list ['b', 'g', 'h']
remQualifier;removing a qualifier (i.e., volatile, const, and restrict) of the variable in the list ['b', 'g', 'h']
addModifier;inserting a modifier (i.e., long, short, signed, and unsigned) of the variable in the list ['b', 'g', 'h']
remModifier;removing a modifier (i.e., long, short, signed, and unsigned) of the variable in the list ['b', 'g', 'h']
repContant;replacing a constant with another valid one of a variable in the list ['b', 'g', 'h'] 
repBinary;replacing a binary operator with another valid one on the variables in the list ['b', 'g', 'h']
repUnary;replacing an unary operator on the variables in the list ['b', 'g', 'h']
remUnary;removing an unary operator on the variables in the list ['b', 'g', 'h']
repUnary;removing an unary operator on the variables in the list ['b', 'g', 'h'] only between line 20 and line 21 in the original given program (if any)
repVar;replacing a variable in the list ['b', 'g', 'h'] with another valid existing one in the list ['b', 'g', 'h']  
addIf;inserting a branch (i.e., if) statement with length  1 - 5  lines in the statement body only between line 20 and line 21 in the original given program and reusing the variables in the list ['b', 'g', 'h'] 
addWhile;inserting a loop (i.e., while or for) statement with length  1 - 5  lines in the statement body only between line 20 and line 21 in the original given program and reusing the variables in the list ['b', 'g', 'h'] 
addFunction;inserting a function call with length  1 - 5  lines in the statement body only between line 20 and line 21 in the original given program and reusing the variables in the list ['b', 'g', 'h'] 
addGoto;inserting a goto statement with length  1 - 5  lines in the statement body only between line 20 and line 21 in the original given program and reusing the variables in the list ['b', 'g', 'h'] 
