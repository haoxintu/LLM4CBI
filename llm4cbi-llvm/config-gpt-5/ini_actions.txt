addQualifier;inserting a qualifier (i.e., volatile, const, and restrict) of the variable in the list ['a', 'b', 'c']
remQualifier;removing a qualifier (i.e., volatile, const, and restrict) of the variable in the list ['a', 'b', 'c']
addModifier;inserting a modifier (i.e., long, short, signed, and unsigned) of the variable in the list ['a', 'b', 'c']
remModifier;removing a modifier (i.e., long, short, signed, and unsigned) of the variable in the list ['a', 'b', 'c']
repContant;replacing a constant with another valid one of a variable in the list ['a', 'b', 'c']
repBinary;replacing a binary operator with another valid one on the variables in the list ['a', 'b', 'c']
repUnary;replacing an unary operator on the variables in the list ['a', 'b', 'c']
remUnary;removing an unary operator on the variables in the list ['a', 'b', 'c']  
repUnary;removing an unary operator on the variables in the list ['a', 'b', 'c'] at the beginning of the main function (if any)
repVar;replacing a variable in the list ['a', 'b', 'c'] with another valid existing one in the list ['a', 'b']
addIf;inserting a branch (i.e., if) statement with length  1 - 5  lines in the statement body at the beginning of the main function  and reusing the variables in the list ['a', 'b'] 
addWhile;inserting a loop (i.e., while or for) statement with length  1 - 5  lines in the statement body at the beginning of the main function and reusing the variables in the list ['a', 'b'] 
addFunction;inserting a function call with length  1 - 5  lines in the statement body at the beginning of the main function and reusing the variables in the list ['a', 'b'] 
addGoto;inserting a goto statement with length  1 - 5  lines in the statement body at the beginning of the main function and reusing the variables in the list ['a', 'b'] 
