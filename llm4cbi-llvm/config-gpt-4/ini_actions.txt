addQualifier;inserting a qualifier (i.e., volatile, const, and restrict) of the variable in the list ['a', 'p', 'q']
remQualifier;removing a qualifier (i.e., volatile, const, and restrict) of the variable in the list ['a', 'p', 'q']
addModifier;inserting a modifier (i.e., long, short, signed, and unsigned) of the variable in the list ['a', 'p', 'q']
remModifier;removing a modifier (i.e., long, short, signed, and unsigned) of the variable in the list ['a', 'p', 'q']
repContant;replacing a constant with another valid one of a variable in the list ['a', 'p', 'q']
repBinary;replacing a binary operator with another valid one on the variables in the list ['a', 'p', 'q']
repUnary;replacing an unary operator on the variables in the list ['a', 'p', 'q']
remUnary;removing an unary operator on the variables in the list ['a', 'p', 'q']  
repUnary;removing an unary operator on the variables in the list ['a', 'p', 'q'] at the beginning of the main function (if any)
repVar;replacing a variable in the list ['a', 'p', 'q'] with another valid existing one in the list ['a']
addIf;inserting a branch (i.e., if) statement with length  1 - 5  lines in the statement body at the beginning of the main function  and reusing the variables in the list ['a'] 
addWhile;inserting a loop (i.e., while or for) statement with length  1 - 5  lines in the statement body at the beginning of the main function and reusing the variables in the list ['a'] 
addFunction;inserting a function call with length  1 - 5  lines in the statement body at the beginning of the main function and reusing the variables in the list ['a'] 
addGoto;inserting a goto statement with length  1 - 5  lines in the statement body at the beginning of the main function and reusing the variables in the list ['a'] 
